const map1 = new Map();
arr=["weryu"]
 function HowManyLetters(word) {
     for(i=0;i<word.length;i++){
        // console.log(word[i]);

         map1.set(word[i], i)
       console.log (map1.get("word[i]"))

     }
    //  word.forEach((element,index)=>
    //     map1.set(element, index));
    //     return map1.get(element)
    //     // console.log(map1.get("element"));
     }
 
var array=HowManyLetters(arr)
        // console.log(array);
